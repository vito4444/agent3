namespace Starsoil.UI
{
    /// <summary>
    /// UI Toolkit shell (top bar, build menu, inspector, alerts) lands with M1/M2 UI tasks
    /// per docs/plan/05. M0 uses only the debug overlay in Game.Presentation.
    /// </summary>
    public static class UiModule
    {
        public const string ModuleName = "Starsoil.UI";
    }
}
